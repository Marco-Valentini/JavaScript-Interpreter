let numbers = [1, 2, 3, 4, 5];
console.log(numbers[2]);          // Access element by index
console.log(numbers.length);      // Array length
numbers.push(6);                 // Add element to the end
console.log(numbers);
numbers.pop();                   // Remove element from the end
console.log(numbers);
